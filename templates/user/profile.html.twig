{% extends "base.html.twig" %}

{% block title %}
    My Profile
{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" href="{{ asset('assets/style/css/user/profile.css') }}">
    <link rel="stylesheet" href="{{ asset('assets/style/css/responsive/600px/user/profile.css') }}">
{% endblock %}

{% block pageTitle %}
    My profile
{% endblock %}
{% block main %}
    <div class="profile">
        <div class="profile__element">
            <div class="profile__label">Last Name</div>
            <div>{{ user.lastName }}</div>
        </div>
        <div class="profile__element">
            <div class="profile__label">First Name</div>
            <div>{{ user.firstName }}</div>
        </div>
        <div class="profile__element">
            <div class="profile__label">Username</div>
            <div>{{ user.username }}</div>
        </div>
        <div class="profile__element">
            <div class="profile__label">Email Adress</div>
            <div>{{ user.emailAddress }}</div>
        </div>
    {#    <div>#}
            {#        <a href="{{ path("users__addition") }}">Add User</a>#}
    {#    </div>#}
        <div class="profile__button">
            <a class="button" href="{{ path( 'profile__modify') }}">Modify My Profile</a>
        </div>
    {#    <div>#}
    {#            <a href="{{ path( 'users_delete', {'id' :user.id}) }}">Delete</a>#}
    {#    </div>#}
    </div>
{% endblock %}